{
  "name": "opsnerve-performance-analyzer",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:migrate": "cd packages/supabase && supabase db push",
    "db:reset": "cd packages/supabase && supabase db reset",
    "db:seed": "cd packages/supabase && supabase db seed"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
